#include <bits/stdc++.h>

int main() {
  int t, n, m;
  std::cin >> t;

  while (t--) {
    std::cin >> n >> m;
    std::vector<int> a(n);

    for (int &ai : a)
      std::cin >> ai;
  }

  return 0;
}
